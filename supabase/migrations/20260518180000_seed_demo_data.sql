-- Seed demo data placeholder.
-- NOTE: This file references existing `profiles.id` values. Replace the SAMPLE_UUID values with actual profile UUIDs from your Supabase project before running.

-- Example:
-- INSERT INTO public.goal_sheets (id, employee_id, cycle_year, status, created_at, updated_at)
-- VALUES ('11111111-1111-1111-1111-111111111111', 'SAMPLE_UUID_EMPLOYEE1', extract(year from now())::int, 'submitted', now(), now());

-- Then insert goals and check_ins referencing the sheet id above.

-- DO NOT RUN this file as-is; replace SAMPLE_UUID_* placeholders with real profile IDs.
